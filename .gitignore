# Dependencies
/node_modules
/.pnp
.pnp.js

# Next.js
/.next/
/out/
/build
next-env.d.ts

# Production
/dist

# Environment variables (NEVER commit secrets — OpenRouter key lives here)
.env
.env*.local
.env.local

# Vercel
.vercel

# Logs / debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
*.pem

# TypeScript
*.tsbuildinfo

# Claude Code — local/personal config stays out of the repo;
# the shared, committed config lives in .claude/settings.json
.claude/settings.local.json

# The challenge brief itself (not part of the product)
Cadre_AI_Chatbot_Take_Home_Candidate.docx.pdf
